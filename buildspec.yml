version: 0.2
phases:
  install:
    runtime-versions:
      java: latest
  build:
    commands:
      - mvn verify sonar:sonar -Dsonar.projectKey=sonarjavadevsecpractisecloud -Dsonar.organization=sonarjavadevsecpractisecloud -Dsonar.host.url=https://sonarcloud.io -Dsonar.token=$SONAR_TOKEN 
